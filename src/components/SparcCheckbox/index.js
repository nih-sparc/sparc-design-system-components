import SparcCheckbox from './src/SparcCheckbox';

/* istanbul ignore next */
SparcCheckbox.install = function(Vue) {
  Vue.component(SparcCheckbox.name, SparcCheckbox);
};

export default SparcCheckbox;
